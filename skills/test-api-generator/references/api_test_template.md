---
program: "{程序名称}"
module: "{模块名称}"
created: "{创建时间，格式：YYYY-MM-DD HH:mm:ss}"
updated: "{修改时间，格式：YYYY-MM-DD HH:mm:ss}"
protocol: "RESTful API / GraphQL / gRPC / WebSocket"
base_url: "https://api.example.com"
tags:
  - API测试
  - 接口用例
---

# API 接口测试用例：{程序名称} - {模块名称}

## 关键链路清单

> 在生成用例前，先识别关键 API 调用链，例如：
> ```
> 电商下单链路：
> POST /api/v1/auth/login → GET /api/v1/products → POST /api/v1/cart → POST /api/v1/orders → POST /api/v1/payments → GET /api/v1/orders/{id}
> ```
> 链路上任一接口失败 = P0 阻断。

---

## 接口依赖图

> 标注接口之间的调用依赖和数据依赖，例如：
> ```
> POST /api/v1/orders
>   ├── 依赖 POST /api/v1/auth/login（需要 auth token）
>   ├── 依赖 GET /api/v1/addresses/{id}（收货地址存在）
>   └── 依赖 GET /api/v1/products/{id}（商品可购买且库存充足）
> ```

---

## 接口测试用例

### {模块名称}

#### 用例 1：{接口简述}-{正向场景}

| 字段 | 内容 |
|------|------|
| **接口** | `{METHOD} /api/v1/xxx` |
| **标题** | `{接口简述}-{正向场景}` |
| **前置条件** | {执行前需要的数据/状态，如"用户已登录，token 有效"} |
| **请求方法** | `{METHOD}` |
| **请求 URL** | `/api/v1/xxx` |
| **请求头** | `Content-Type: application/json`<br>`Authorization: Bearer <具体token值>` |
| **请求体** | ```json { "field1": "具体值", "field2": 123 } ``` |
| **预期状态码** | `200` |
| **预期响应体** | ```json { "code": 0, "message": "success", "data": { "id": 1, "field1": "具体值" } } ``` |
| **预期响应时间** | `< 500ms` |
| **优先级** | P0 |

#### 用例 2：{接口简述}-{异常场景，如参数校验失败}

| 字段 | 内容 |
|------|------|
| **接口** | `{METHOD} /api/v1/xxx` |
| **标题** | `{接口简述}-{异常场景}` |
| **前置条件** | {执行前需要的数据/状态} |
| **请求方法** | `{METHOD}` |
| **请求 URL** | `/api/v1/xxx` |
| **请求头** | `Content-Type: application/json` |
| **请求体** | ```json { "field1": "" } ``` |
| **预期状态码** | `400` |
| **预期响应体** | ```json { "code": 40001, "message": "field1 不能为空", "data": null } ``` |
| **预期响应时间** | `< 200ms` |
| **优先级** | P1 |

#### 用例 3：{接口简述}-{鉴权失败}

| 字段 | 内容 |
|------|------|
| **接口** | `{METHOD} /api/v1/xxx` |
| **标题** | `{接口简述}-{鉴权失败}` |
| **前置条件** | {如"用户未登录或 token 已过期"} |
| **请求方法** | `{METHOD}` |
| **请求 URL** | `/api/v1/xxx` |
| **请求头** | `Content-Type: application/json`<br>`Authorization: Bearer expired_token` |
| **请求体** | ```json { "field1": "具体值" } ``` |
| **预期状态码** | `401` |
| **预期响应体** | ```json { "code": 40101, "message": "token 已过期", "data": null } ``` |
| **预期响应时间** | `< 200ms` |
| **优先级** | P1 |

#### 用例 4：{接口简述}-{业务状态非法流转}

| 字段 | 内容 |
|------|------|
| **接口** | `{METHOD} /api/v1/xxx` |
| **标题** | `{接口简述}-{当前状态: X, 触发操作: Y}` |
| **前置条件** | {如"订单 ID=1001 当前状态为 CLOSED"} |
| **当前状态** | `CLOSED` |
| **触发操作** | `调用支付接口 POST /api/v1/payments` |
| **请求方法** | `{METHOD}` |
| **请求 URL** | `/api/v1/xxx` |
| **请求头** | `Content-Type: application/json`<br>`Authorization: Bearer <具体token值>` |
| **请求体** | ```json { "order_id": 1001 } ``` |
| **预期状态码** | `409` |
| **预期响应体** | ```json { "code": 40901, "message": "订单已关闭，无法支付", "data": null } ``` |
| **优先级** | P1 |

---

## 接口覆盖矩阵

| 接口 | 正向 | 参数校验 | 边界值 | 异常 | 鉴权 | 幂等 | 状态流转 | 性能 | 用例总数 |
|------|:----:|:--------:|:------:|:----:|:----:|:----:|:--------:|:----:|:--------:|
| `POST /api/v1/users/login` | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ | 5 |
| `GET /api/v1/orders/{id}` | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ | ✅ | ❌ | 4 |

> ✅ = 已覆盖  ❌ = 未覆盖

---

## 功能依赖图

> 标注测试用例之间的执行顺序和前置数据依赖，例如：
> ```
> 支付接口用例
>   ├── 依赖 登录接口用例（获取 auth token）
>   ├── 依赖 创建订单接口用例（生成待支付订单）
>   └── 依赖 业务状态用例（验证 CLOSED 状态不可支付）
> ```
> 被依赖的接口用例必须先执行，为其生成必要的测试数据。

---

## 覆盖度自检清单

| 维度 | 覆盖情况 | 对应用例 |
|------|---------|---------|
| 正向流程 | ✅/⚠️ | |
| 参数校验 | ✅/⚠️ | |
| 边界值 | ✅/⚠️ | |
| 异常流程 | ✅/⚠️ | |
| 鉴权与权限 | ✅/⚠️ | |
| 幂等性 | ✅/⚠️ | |
| 业务状态流转 | ✅/⚠️ | |
| 安全基线 | ✅/⚠️ | |
| 性能基线 | ✅/⚠️ | |

## 迭代摘要

| 指标 | 值 |
|------|-----|
| 初始生成用例数 | |
| 批判发现缺陷数 | |
| 修复/新增用例数 | |
| 最终用例数 | |
| 接口覆盖率 | |
| 遗留问题 | |

## 需求缺口清单

| 序号 | 缺口描述 | 需确认的问题 |
|------|---------|-------------|
| 1 | | |
